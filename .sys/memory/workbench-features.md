# Workbench Features Critical Memory

This is not a progress log. The workbench-features planner reads it; the workbench-features executor alone may append a durable learning.

Admit only a reusable constraint, surprising failure mode, cross-cycle dependency pattern, or verification technique that will materially change future decisions.

## Entries

None yet.
