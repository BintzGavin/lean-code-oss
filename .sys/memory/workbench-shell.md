# Workbench Shell Critical Memory

This is not a progress log. The workbench-shell planner reads it; the workbench-shell executor alone may append a durable learning.

Admit only a reusable constraint, surprising failure mode, cross-cycle dependency pattern, or verification technique that will materially change future decisions.

## Entries

None yet.
